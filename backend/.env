# Database
DB_HOST=192.168.0.106
DB_PORT=5432
DB_NAME=ai_system
DB_USER=postgres
DB_PASSWORD=090325

# Ollama (llama3.1)
OLLAMA_BASE_URL=http://localhost:11434
LLM_MODEL_NAME=llama3.1:latest
LLM_PROVIDER=ollama

# CORS settings
CORS_ORIGINS=http://localhost:8000,https://yourdomain.com,https://app.yourdomain.com

# API Key for authentication
API_KEY=09032005
REQUIRE_API_KEY=true  # true = bật authentication, false = tắt (development)

# Rate Limiting settings
RATE_LIMIT_ENABLED=true  # true = bật, false = tắt
DEFAULT_RATE_LIMIT=100/minute  # Format: "number/period" (second, minute, hour, day)
STRICT_RATE_LIMIT=10/minute  # Cho các endpoints quan trọng

# PostgreSQL Extensions
USE_PGVECTOR=true

# Redis Configuration
REDIS_ENABLED=true
REDIS_HOST=192.168.0.106
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=  # Optional
REDIS_DEFAULT_TTL=3600  # 1 hour default
LOG_LEVEL=DEBUG
